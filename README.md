# stable_diffusion_experiments

Quick scripts to test different stable diffusion models

## instruct-pix2pix ##

* Query for images in a loop, so model does not need to reload every time.
* timbrooks/instruct-pix2pix
